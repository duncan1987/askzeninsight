---
name: "zh-locale-username-password-auth-with-admin-approval"
overview: "Replace Logto/WeChat auth with Supabase username+password for zh locale only (en locale completely unchanged). Include admin approval flow and unlimited conversation quota for approved zh users. Uses synthetic email mapping for Supabase Auth compatibility."
todos:
  - Generate PRD markdown to docs/ZH-AUTH-PRD.md
  - Add username + account_status columns migration to profiles table
  - Update handle_new_user trigger to store username and account_status
  - Create password validation utility (lib/password.ts)
  - Create username validation utility (lib/username.ts)
  - Create server-side register API route (app/api/auth/register/route.ts)
  - Create server-side sign-in API route (app/api/auth/sign-in/route.ts)
  - Create admin registration review API route (app/api/admin/user-review/route.ts)
  - Create admin registration review page (app/admin/users/page.tsx)
  - Create sign-in/register page (app/[locale]/auth/sign-in/page.tsx)
  - Modify lib/subscription.ts to add 'approved_zh' tier with unlimited quota
  - Modify lib/usage-limits.ts to support unlimited quota for approved zh users
  - Update SignInButton to redirect zh locale to sign-in page (en locale unchanged)
  - Update SignOutButton to remove Logto sign-out logic
  - Add auth translation keys to messages/en.json and messages/zh.json
  - Remove Logto dependency and routes
  - Update .env.example to remove Logto vars
---

# PRD: 中文环境用户名+密码登录方案（含管理员审核 + 无限额度）

## Goal

在中文语言环境下，用 Supabase Auth 用户名+密码登录替代 Logto/微信登录。**英文环境的 Google OAuth 完全不变，Google 用户仍需订阅才能获得 Pro 额度**。中文环境新用户注册后需管理员审核通过才能登录，**审核通过后无需订阅即可畅享无限对话额度**。注册成功后浏览器自动记住账号密码，下次访问一键登录。

## Scope / Non-goals

### In Scope
- 新建登录/注册页面（**仅 zh locale**）
- 用户名+密码注册（含昵称，不要求邮箱）
- 用户名+密码登录（**需审核通过**）
- 管理员审核注册申请（批准/拒绝）
- **审核通过 zh 用户无限对话额度**（不限制每日消息数）
- 密码强度校验（实时反馈）
- 浏览器密码管理器自动填充支持
- SignInButton zh locale 行为改为跳转登录页（en locale 不变）
- SignOutButton 移除 Logto 登出逻辑
- i18n 翻译（auth namespace 扩展）
- 移除 Logto 依赖及相关代码
- 数据库迁移：profiles 表增加 `username` + `account_status` 列
- 订阅系统改造：新增 `approved_zh` 层级

### Non-goals
- 手机号+验证码登录（已否决）
- 微信登录（已否决）
- 忘记密码/重置密码流程（后续迭代，本次预留入口）
- en locale 登录方式变更（**完全不变**）
- en locale 订阅机制变更（**Google 用户仍需 Creem 订阅才能 Pro**）
- 邮箱登录（系统内部使用合成邮箱）
- 拒绝后重新注册（被拒绝的用户名不可重新注册）
- zh 用户 Creem 订阅功能（zh 用户无需订阅）

## ⚠️ en 用户不受影响保证（关键约束）

**Google OAuth 用户（en locale）完全不受本次变更影响**，通过以下五层保障实现：

### 保障 1：数据库默认值
```sql
account_status TEXT NOT NULL DEFAULT 'approved'
```
- Google 用户注册时，`handle_new_user()` 触发器创建 profile 行
- 触发器逻辑：`COALESCE(NEW.raw_user_meta_data->>'account_status', 'approved')`
- Google OAuth 不传 `account_status` metadata，所以 **Google 用户自动为 `approved'**

### 保障 2：登录路径完全隔离
- **Google 用户**：走 Supabase 原生 OAuth → `/auth/callback/route.ts` → **不检查 `account_status`** → 直接登录
- **zh 用户名用户**：走 `POST /api/auth/sign-in` → **检查 `account_status`** → pending/rejected/approved
- **两条路径完全不交叉**

### 保障 3：订阅层级隔离
- **en 用户**：tier 由 Creem 订阅决定 → `free`（无订阅）或 `pro`（有订阅）
- **zh 用户**：tier 由 `account_status + username` 决定 → `approved_zh`（审核通过，无需订阅）
- `approved_zh` 仅在 `username IS NOT NULL` 时生效，Google 用户无 username，不会被误判

### 保障 4：代码分支隔离
```typescript
// SignInButton - en 分支完全不变
if (locale === 'zh') {
  router.push(`/${locale}/auth/sign-in`)  // zh: 跳转登录页
} else {
  await handleGoogleSignIn(e)              // en: Google OAuth，完全不变
}
```

### 保障 5：不修改的文件
| 文件 | 说明 |
|------|------|
| `app/auth/callback/route.ts` | Google OAuth 回调，不修改 |
| `components/auth/sign-in-button.tsx` (en 分支) | handleGoogleSignIn() 不变 |
| `lib/creem.ts` | 支付配置，不修改 |
| `app/api/creem/` | 支付相关路由，不修改 |

## 用户层级体系

### 改造前
| Tier | 每日额度 | API Key | 保存历史 | 适用用户 |
|------|---------|---------|---------|---------|
| anonymous | 10 | ZHIPU_API_FREE | No | 未登录用户 |
| free | 10 | ZHIPU_API_FREE | No | en 已登录无订阅 |
| pro | 30 | ZHIPU_API_KEY | Yes | en 有 Creem 订阅 |

### 改造后
| Tier | 每日额度 | API Key | 保存历史 | 适用用户 |
|------|---------|---------|---------|---------|
| anonymous | 10 | ZHIPU_API_FREE | No | 未登录用户 |
| free | 10 | ZHIPU_API_FREE | No | en 已登录无订阅 |
| pro | 30 | ZHIPU_API_KEY | Yes | en 有 Creem 订阅 |
| **approved_zh** | **无限** | **ZHIPU_API_KEY** | **Yes** | **zh 审核通过用户** |

### 层级判定逻辑

```
getUserSubscription(userId):
  if 无 userId → anonymous
  if userId 存在:
    查询 profiles (admin client): username, account_status
    if username IS NOT NULL AND account_status = 'approved':
      → approved_zh（无限额度，付费 API，保存历史）
    else:
      → 查 Creem 订阅 → 有订阅? pro : free
```

**关键**：`approved_zh` 判定优先于 Creem 订阅查询。zh 用户无需 Creem 订阅即可获得无限额度。

## Design

### 整体架构

```
=== zh 用户注册流程 ===
zh 用户点击「登录」→ 跳转 /[locale]/auth/sign-in → 注册 tab
→ 填写账号名+昵称+密码+确认密码 → POST /api/auth/register
→ account_status='pending' → 显示"等待管理员审核"

=== zh 用户登录流程 ===
zh 用户访问登录页 → 填写账号名+密码 → POST /api/auth/sign-in
→ 检查 account_status:
  approved → 登录成功，tier='approved_zh'，无限额度
  pending → "审核中"
  rejected → "已拒绝"

=== zh 用户对话流程 ===
zh 审核通过用户发消息 → getUserSubscription → approved_zh
→ checkUsageLimit: 无限制（canProceed = true, limit = Infinity）
→ 使用付费 API Key (ZHIPU_API_KEY)
→ 保存对话历史

=== en 用户流程（完全不变）===
en 用户点击 Sign In → Google OAuth → 直接登录
→ getUserSubscription → free/pro（由 Creem 订阅决定）
→ free: 10条/天 | pro: 30条/天

=== 管理员审核流程 ===
管理员访问 /admin/users → ADMIN_SECRET_KEY → 批准/拒绝
→ 批准: account_status='approved' → 用户可登录 + 无限额度
→ 拒绝: account_status='rejected' → 用户名保留
```

### 1. 合成邮箱映射机制

| 概念 | 值 |
|------|------|
| 用户输入 | `myname` |
| Supabase auth.users.email | `myname@users.internal` |
| profiles.username | `myname` |
| profiles.email | `myname@users.internal` |

**设计要点**：
- `@users.internal` 后缀标识合成邮箱，与 en 用户真实 Gmail 不冲突
- 用户名统一转小写存储和查找
- Supabase Dashboard 需关闭 "Confirm email"
- **en 用户通过 Google OAuth 注册，email 是真实地址，不受影响**

### 2. 数据库变更

**新增迁移** `supabase/migrations/20260901_add_username_and_account_status.sql`：

```sql
-- Add username column to profiles
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS username TEXT UNIQUE;

-- Add account_status column to profiles
-- DEFAULT 'approved' ensures existing Google OAuth users are unaffected
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS account_status TEXT NOT NULL DEFAULT 'approved'
  CHECK (account_status IN ('pending', 'approved', 'rejected'));

-- Create indexes
CREATE INDEX IF NOT EXISTS profiles_username_idx ON public.profiles(username);
CREATE INDEX IF NOT EXISTS profiles_account_status_idx ON public.profiles(account_status);

-- Update handle_new_user trigger
-- COALESCE(metadata->>'account_status', 'approved') ensures:
--   Google OAuth users (no account_status in metadata) → 'approved'
--   zh username users (account_status='pending' in metadata) → 'pending'
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email, full_name, avatar_url, username, account_status)
  VALUES (
    NEW.id,
    NEW.email,
    COALESCE(NEW.raw_user_meta_data->>'name', NEW.raw_user_meta_data->>'full_name', ''),
    NEW.raw_user_meta_data->>'avatar_url',
    NEW.raw_user_meta_data->>'username',
    COALESCE(NEW.raw_user_meta_data->>'account_status', 'approved')
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = public;
```

**注意**：`usage_records` 表的 `user_tier` CHECK 约束需更新以支持 `approved_zh`：

```sql
-- Update user_tier constraint to include 'approved_zh'
ALTER TABLE public.usage_records DROP CONSTRAINT IF EXISTS usage_records_user_tier_check;
ALTER TABLE public.usage_records ADD CONSTRAINT usage_records_user_tier_check
  CHECK (user_tier IN ('anonymous', 'free', 'pro', 'approved_zh'));
```

### 3. 用户名校验规则

| 规则 | 要求 |
|------|------|
| 长度 | 3-20 个字符 |
| 字符集 | 仅允许字母（a-z, A-Z）、数字（0-9）、下划线（_） |
| 唯一性 | 不与已有用户名重复（大小写不敏感） |
| 保留词 | 禁止 `admin`、`system`、`root`、`support`、`help`、`moderator` |

### 4. 密码强度规则

| 规则 | 要求 |
|------|------|
| 最小长度 | ≥ 8 字符 |
| 包含字母 | 至少 1 个英文字母 |
| 包含数字 | 至少 1 个数字 |

### 5. 注册 API（zh 专用）

**路由**: `POST /api/auth/register`

**流程**:
1. 服务端校验用户名规则 + 密码强度 + 唯一性
2. 调用 `admin.createUser`：
   ```typescript
   await adminClient.auth.admin.createUser({
     email: `${username.toLowerCase()}@users.internal`,
     password,
     email_confirm: true,
     user_metadata: {
       username: username.toLowerCase(),
       full_name: nickname,
       account_status: 'pending',
     },
   })
   ```
3. 触发器自动创建 profile（`account_status='pending'`）
4. 返回成功（无 session，需等审核）

### 6. 登录 API（zh 专用）

**路由**: `POST /api/auth/sign-in`

**流程**:
1. 查询 `profiles.username` → 获取 UUID + account_status
2. 用户不存在 → "账号或密码错误"
3. `account_status` 检查：
   - `pending` → HTTP 403 "审核中"
   - `rejected` → HTTP 403 "已拒绝"
   - `approved` → 继续
4. 获取合成邮箱 → `signInWithPassword` → 设置 session → 返回成功

### 7. 订阅系统改造

#### 7.1 `lib/subscription.ts` 改造

```typescript
export type UserTier = 'anonymous' | 'free' | 'pro' | 'approved_zh'

export async function getUserSubscription(userId?: string): Promise<SubscriptionInfo> {
  if (!userId) {
    return { tier: 'anonymous', plan: null, model: 'glm-5', apiKey: ZHIPU_API_FREE, saveHistory: false }
  }

  // === 新增：检查 zh 审核通过用户 ===
  const adminClient = createAdminClient()
  if (adminClient) {
    const { data: profile } = await adminClient
      .from('profiles')
      .select('username, account_status')
      .eq('id', userId)
      .single()

    // 有 username 且 account_status='approved' → zh 审核通过用户
    if (profile?.username && profile.account_status === 'approved') {
      return {
        tier: 'approved_zh',
        plan: null,           // 无 Creem 订阅计划
        model: 'glm-5',
        apiKey: ZHIPU_API_KEY,  // 使用付费 API Key
        saveHistory: true,       // 保存对话历史
      }
    }
  }
  // === 新增结束 ===

  // 以下逻辑完全不变：查 Creem 订阅 → free/pro
  // ...原有代码...
}
```

**关键点**：
- `approved_zh` 判定**优先于** Creem 订阅查询
- Google 用户无 `username`（profiles.username 为 NULL），不会进入 `approved_zh` 分支
- en 用户走原有 free/pro 逻辑，完全不受影响

#### 7.2 `lib/usage-limits.ts` 改造

**新增常量**：
```typescript
export const USAGE_LIMITS = {
  ANONYMOUS_DAILY: 10,
  FREE_DAILY: 10,
  PRO_DAILY: 30,
  APPROVED_ZH_DAILY: Infinity,  // 无限制
}
```

**`checkUsageLimit` 改造**：
```typescript
// 新增 approved_zh 分支
if (subscription.tier === 'approved_zh') {
  return { canProceed: true, limit: Infinity, remaining: Infinity }
}
```

**`isWithinPremiumQuota` 改造**：
```typescript
// approved_zh 用户始终使用付费模型，无降级
if (subscription.tier === 'approved_zh') {
  return true
}
```

**`recordUsage` 改造**：
```typescript
// 新增 approved_zh tier 记录
userTier = subscription.tier  // 'approved_zh' 会被正确记录到 usage_records
```

**`getUsageStats` 改造**：
```typescript
// approved_zh 返回无限额度统计
if (subscription.tier === 'approved_zh') {
  return { used, limit: Infinity, remaining: Infinity, percentage: 0 }
}
```

#### 7.3 `app/api/chat/route.ts` 改造

**公平使用策略调整**：
```typescript
// Pro 用户超出30条降级为免费模型
// approved_zh 用户不降级，始终使用付费模型
if (subscription.tier === 'pro') {
  const withinPremiumQuota = await isWithinPremiumQuota(userId)
  if (!withinPremiumQuota) {
    model = 'glm-5'  // 降级（虽模型名相同，但用免费 key）
    isPremiumModel = false
  }
}
// approved_zh 用户无需检查额度，isWithinPremiumQuota 已返回 true
```

### 8. 管理员审核 API

**路由**: `app/api/admin/user-review/route.ts`

**鉴权**: `x-admin-key` = `ADMIN_SECRET_KEY`

**GET /api/admin/user-review?status=pending**
- 查询 profiles 指定 account_status 的用户列表

**POST /api/admin/user-review**
- `{ userId, action: 'approve' | 'reject', notes? }`
- 批准：`account_status = 'approved'` → 用户可登录 + 无限额度
- 拒绝：`account_status = 'rejected'` → 用户名保留

### 9. 管理员审核页面

**路径**: `app/admin/users/page.tsx`

复用 `app/admin/refunds/page.tsx` 模式：
- ADMIN_SECRET_KEY 认证
- 统计卡片：待审核 / 已批准 / 已拒绝
- Tab 切换 + 用户列表 + 批准/拒绝按钮

### 10. 登录/注册页面

**路径**: `app/[locale]/auth/sign-in/page.tsx`（仅 zh locale）

**登录表单**:
| 字段 | type | name | autocomplete |
|------|------|------|-------------|
| 账号 | text | username | username |
| 密码 | password | password | current-password |

**注册表单**:
| 字段 | type | name | autocomplete |
|------|------|------|-------------|
| 账号 | text | username | username |
| 昵称 | text | nickname | name |
| 密码 | password | password | new-password |
| 确认密码 | password | confirmPassword | new-password |

**关键交互**：
- 注册成功 → "注册成功！请等待管理员审核通过后方可登录。"
- 登录时 pending → "账号审核中，请等待管理员批准"
- 登录时 rejected → "注册申请已被拒绝"
- 登录成功 → `router.push('/')` → 浏览器保存密码

### 11. 前端额度展示调整

**UsageMeter 组件**：zh approved_zh 用户显示"无限额度"而非具体数字。
**Dashboard 页面**：zh 用户不显示订阅信息和升级按钮。

### 12. SignInButton 修改

**zh locale**: 跳转登录页
**en locale**: 不变，仍走 Google OAuth

### 13. SignOutButton 修改

移除 Logto 登出逻辑。

### 14. Logto 移除

| 操作 | 说明 |
|------|------|
| 删除 `app/api/auth/logto/` | 三个路由 |
| 删除 `lib/logto.ts` | 配置文件 |
| 卸载 `@logto/next` | |
| 移除 `.env.example` Logto 变量 | |
| 保留 `profiles.logto_id` | 不迁移 |

### 15. i18n 扩展

**zh.json auth namespace 新增 key**:
```json
{
  "register": "注册",
  "signInTab": "登录",
  "registerTab": "注册",
  "username": "账号",
  "usernamePlaceholder": "请输入账号",
  "nickname": "昵称",
  "nicknamePlaceholder": "请输入昵称",
  "password": "密码",
  "passwordPlaceholder": "请输入密码",
  "confirmPassword": "确认密码",
  "confirmPasswordPlaceholder": "请再次输入密码",
  "passwordMinLength": "至少 8 个字符",
  "passwordRequireLetter": "至少包含 1 个字母",
  "passwordRequireNumber": "至少包含 1 个数字",
  "passwordStrength": "密码强度",
  "passwordTooWeak": "密码不符合要求",
  "passwordMismatch": "两次密码不一致",
  "usernameInvalid": "账号仅支持字母、数字和下划线，3-20个字符",
  "usernameReserved": "该账号不可使用",
  "usernameExists": "该账号已被注册",
  "nicknameRequired": "请输入昵称",
  "nicknameTooShort": "昵称至少 2 个字符",
  "nicknameTooLong": "昵称最多 20 个字符",
  "passwordRequired": "请输入密码",
  "registerSuccess": "注册成功",
  "registerPendingApproval": "注册成功！请等待管理员审核通过后方可登录。",
  "registerFailed": "注册失败",
  "accountPending": "账号审核中，请等待管理员批准",
  "accountRejected": "注册申请已被拒绝",
  "invalidCredentials": "账号或密码错误",
  "alreadyHaveAccount": "已有账号？",
  "noAccount": "还没有账号？",
  "goToRegister": "去注册",
  "goToSignIn": "去登录",
  "backToHome": "返回首页",
  "forgotPassword": "忘记密码？",
  "forgotPasswordHint": "功能开发中",
  "usernameRequired": "请输入账号",
  "unlimitedQuota": "无限额度"
}
```

**en.json**: 对应英文翻译。

### 16. 已有 Logto 用户迁移

| 场景 | 处理 |
|------|------|
| Logto 用户 | account_status 默认 approved，但无密码无法登录，需"忘记密码" |
| Google OAuth 用户 | 完全不受影响，free/pro 由 Creem 订阅决定 |
| Logto 用户 username | 为 NULL，不会被误判为 approved_zh |

## Key Files

| 文件 | 操作 | 说明 |
|------|------|------|
| `docs/ZH-AUTH-PRD.md` | 新建 | PRD 文档 |
| `supabase/migrations/20260901_add_username_and_account_status.sql` | 新建 | 数据库迁移 |
| `lib/password.ts` | 新建 | 密码校验 |
| `lib/username.ts` | 新建 | 用户名校验 |
| `app/api/auth/register/route.ts` | 新建 | 注册 API |
| `app/api/auth/sign-in/route.ts` | 新建 | 登录 API |
| `app/api/admin/user-review/route.ts` | 新建 | 管理员审核 API |
| `app/admin/users/page.tsx` | 新建 | 管理员审核页面 |
| `app/[locale]/auth/sign-in/page.tsx` | 新建 | 登录/注册页面 |
| `lib/subscription.ts` | **修改** | 新增 approved_zh tier |
| `lib/usage-limits.ts` | **修改** | 新增无限额度逻辑 |
| `app/api/chat/route.ts` | **微调** | approved_zh 无降级 |
| `components/auth/sign-in-button.tsx` | 修改 | zh 跳转登录页 |
| `components/auth/sign-out-button.tsx` | 修改 | 移除 Logto |
| `messages/zh.json` | 修改 | 扩展 auth namespace |
| `messages/en.json` | 修改 | 扩展 auth namespace |
| `app/api/auth/logto/` | 删除 | Logto 路由 |
| `lib/logto.ts` | 删除 | Logto 配置 |
| `.env.example` | 修改 | 移除 Logto 变量 |
| `app/auth/callback/route.ts` | **不修改** | Google OAuth 回调 |
| `lib/creem.ts` | **不修改** | 支付配置 |
| `app/api/creem/` | **不修改** | 支付路由 |

## Execution Sequence

1. **生成 PRD 文档** — `docs/ZH-AUTH-PRD.md`
2. **数据库迁移** — `supabase/migrations/20260901_add_username_and_account_status.sql`
3. **工具函数** — `lib/password.ts` + `lib/username.ts`
4. **注册 API** — `app/api/auth/register/route.ts`
5. **登录 API** — `app/api/auth/sign-in/route.ts`
6. **管理员审核 API** — `app/api/admin/user-review/route.ts`
7. **管理员审核页面** — `app/admin/users/page.tsx`
8. **订阅系统改造** — `lib/subscription.ts`（新增 approved_zh）
9. **额度系统改造** — `lib/usage-limits.ts`（无限额度）
10. **Chat API 微调** — `app/api/chat/route.ts`（approved_zh 无降级）
11. **i18n** — `messages/zh.json` + `messages/en.json`
12. **登录/注册页面** — `app/[locale]/auth/sign-in/page.tsx`
13. **SignInButton 修改** — zh 跳转登录页
14. **SignOutButton 修改** — 移除 Logto
15. **删除 Logto** — 路由 + 配置 + 依赖
16. **清理 .env.example**

## Verification

| 检查项 | 方式 |
|--------|------|
| TypeScript | `npx tsc --noEmit` |
| ESLint | `pnpm lint` |
| 构建 | `pnpm build` |
| zh 注册+审核+登录 | 完整流程验证 |
| zh 无限额度 | 审核通过用户发送 >30 条消息 |
| zh 使用付费 API Key | 检查 chat route 日志确认 apiKey |
| zh 保存历史 | 审核通过用户对话可回顾 |
| zh pending 拦截 | 未审核用户无法登录 |
| zh rejected 拒绝 | 被拒绝用户无法登录 |
| **en Google OAuth 不变** | en 登录 + 订阅 + 额度 10/30 |
| **en 无订阅=free** | en 用户无 Creem 订阅 → 10条/天 |
| **en 有订阅=pro** | en 用户有 Creem 订阅 → 30条/天 |
| 登出 | zh/en 均正常 |
| 浏览器密码保存 | zh 登录成功后浏览器提示保存 |
| 管理员审核页 | /admin/users 正常工作 |

## Risks And Compatibility

| 风险 | 影响 | 缓解 |
|------|------|------|
| zh 用户滥用无限额度 | 中 | 管理员审核是第一道门槛；可后续添加单日异常检测 |
| 付费 API Key 成本上升 | 中 | zh 用户使用 ZHIPU_API_KEY，调用成本需监控 |
| 已有 Logto 用户 | 中 | 无密码无法登录，需"忘记密码" |
| Supabase "Confirm email" | 高 | 必须关闭 |
| en 用户受影响 | **无** | 五层保障（见专节） |
| `approved_zh` tier 与 Creem 冲突 | 无 | zh 用户不经过 Creem，en 用户不触发 approved_zh |

## Rollback

1. `git revert` 相关 commits
2. `pnpm add @logto/next` + 恢复 .env.example
3. 恢复 `lib/subscription.ts` 和 `lib/usage-limits.ts` 原版
4. 可选：`ALTER TABLE profiles DROP COLUMN username; DROP COLUMN account_status;`
5. 恢复 `handle_new_user()` 原版
6. 恢复 usage_records 的 user_tier CHECK 约束
7. en 用户全程不受影响
