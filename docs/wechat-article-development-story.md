# 开发手记：AI禅修助手"Ask Zen Insight"的产品打磨之路

## 前言

作为一名开发者，我最近在做一款AI禅修助手产品"Ask Zen Insight"——一个基于禅宗哲学的AI精神伴侣。产品还在开发打磨阶段，今天想和大家分享这段**从MVP到可用产品**的打磨过程，重点聊聊**订阅系统的重构、多轮Bug修复、以及合规性建设**的心得。

---

## 一、聊天功能的多轮Bug修复

### 1.1 第一版：对话能用，但体验糟糕

**初始实现：**
```typescript
// 聊天接口简单粗暴
const response = await fetch("/api/chat", {
  body: JSON.stringify({ messages })
})

const data = await response.json()
setMessages(prev => [...prev, data.message])
```

**问题暴露：**
- ❌ 用户发消息后要等10秒才看到回复
- ❌ 无法感知AI是否在思考
- ❌ 网络错误时提示"API error"

### 1.2 流式响应的实现

**改造方案：使用Server-Sent Events**

```typescript
// TransformStream转换流式数据
const transformStream = new TransformStream({
  transform(chunk, controller) {
    const text = decoder.decode(chunk)
    // 解析SSE格式，逐字输出
    controller.enqueue(new TextEncoder().encode(content))
  }
})
```

**效果：**
- ✅ 用户看到AI像真人一样逐字回复
- ✅ "山不转水转"慢慢显现，更有禅意
- ✅ 首字响应时间从10秒降到0.5秒

### 1.3 Bug #1：重复创建Conversation记录

**问题发现：**
测试时发现月付用户发1条消息，Chat History生成1条记录，但又创建了1条"New Conversation"记录。

**排查过程：**
```typescript
// 问题代码
await saveMessage("user", userInput)
// React状态异步更新，currentConversationId还是undefined
await saveMessage("assistant", fullResponse) // 又创建了新conversation
```

**修复方案：**
```typescript
// 用返回值而非依赖状态
const conversationId = await saveMessage("user", userInput)

// 明确传入conversationId
await saveMessage("assistant", fullResponse, conversationId)
```

**教训：** React状态更新是异步的，不要在下一个await依赖它。

### 1.4 Bug #2：禅意错误提示

**背景：** API调用偶尔会失败，原错误提示"API error: timeout"太技术化。

**改造方案：**
```typescript
const ZEN_ERRORS = [
  "Mountains remain silent through storms. Please try again.",
  "The bamboo bends but does not break. Let us reconnect.",
  "In stillness, clarity returns. Breathe and try once more.",
  // ... 8种随机消息
]
```

**效果：** 错误不再是冷冰冰的技术术语，而是符合产品调性的禅意提示。

> **细节打磨：** 错误提示也是产品体验的一部分。

---

## 二、订阅系统的三次重构

### 2.1 第一版：简单粗暴

**初始设计：**
- 月付：$2.99/月
- 年付：$24.99/年（省30%）
- 功能：30条消息/天

**问题：**
- 用户不敢直接订阅（怕浪费）
- 没说清楚超额后怎么办
- 30条用完就硬限制？

### 2.2 第二版：引入Fair Use Policy

**核心思路：**
> Pro用户前30条消息用高级AI（GLM-4），超额后降级到基础AI（glm-4-flash），但继续可用。

**代码实现：**
```typescript
// 检查是否在优质配额内
const withinPremiumQuota = await isWithinPremiumQuota(userId)

if (!withinPremiumQuota) {
  // 降级到基础模型
  model = 'glm-4-flash'
  fairUseNotice = 'You have exceeded your daily premium quota...'
}
```

**效果：**
- ✅ 用户敢订阅了（知道超额还能用）
- ✅ 避免滥用（优质模型有配额）
- ✅ 体验不中断（基础模型无限用）

### 2.3 第三版：退款政策的透明化

**痛点：** 用户怕订阅了后悔。

**方案：**
- 48小时内全额退款（≤5条消息）
- 按实际使用量比例退款
- Dashboard一键取消

**关键代码：**
```typescript
// 只统计user消息，不算assistant（重要！）
const { data: usageRecords } = await adminClient
  .from('usage_records')
  .select('id')
  .eq('user_id', userId)
  .eq('message_type', 'user')  // ← 这里是关键
  .gte('timestamp', today.toISOString())

// 按比例计算退款
const daysUsed = Math.ceil(usageCount / 100)
const refundPercent = ((30 - daysUsed) / 30) * 100
```

### 2.4 Bug #3：Usage统计翻倍

**问题发现：** 用户发了4条消息，系统提示用了8条。

**根因：**
```typescript
// 错误代码
const usageCount = await supabase
  .from('usage_records')
  .select('*')
// 统计了user + assistant，所以翻倍
```

**修复：**
```typescript
// 正确代码
.eq('message_type', 'user')  // 只算用户消息
```

**教训：** 退款计算涉及用户钱袋子，必须精确！

### 2.5 Bug #4：Subscription_id为NULL

**问题：** usage_records表中subscription_id都是空白。

**根因：**
```typescript
// 查询条件太严
.in('status', ['active', 'cancelled', 'canceled'])
.gte('current_period_end', new Date().toISOString())
// 时间过滤导致查不到刚创建的订阅
```

**修复：**
```typescript
// 简化查询逻辑
const activeSub = subRecords?.find((sub) =>
  sub.status === 'active' && !sub.replaced_by_new_plan
)
```

---

## 三、产品合规性建设

### 3.1 服务条款（Terms of Service）

**核心条款设计：**

**1. 明确产品定位**
```
"本服务是娱乐性和灵性探索工具，不构成医疗建议、心理咨询或专业诊断。"
```

**2. AI模型成本调整条款（新增）**
```
"订阅费用可能根据第三方AI提供商的成本变化进行调整。
任何调整将提前30天通知现有订阅者。"
```

**3. Fair Use Policy说明**
```
"为确保服务质量，Pro计划前30条消息/天使用高级AI。
超额后自动切换到基础AI，但服务继续可用。"
```

**4. 退款政策**
```
- 48小时内：≤5条消息全额退款
- 48小时后：按使用量比例退款
- 7天内：可取消（需审核）
```

### 3.2 隐私政策（Privacy Policy）

**核心内容：**

**1. 数据收集**
- 账户信息：邮箱、姓名
- 使用数据：消息计数（用于计费）
- 聊天记录：仅Pro用户，且永久保存

**2. 数据使用**
- ❌ 不用于训练AI模型
- ❌ 不出售给第三方
- ✅ 仅用于提供服务

**3. 用户权利**
- 查看：Dashboard查看使用情况
- 删除：可删除单个对话
- 注销：联系support删除账户

**4. Cookie和追踪**
- 必要Cookie：认证状态
- 分析Cookie：Vercel Analytics
- 可选择禁用（不影响功能）

### 3.3 退款政策（Refund Policy）独立页面

**为什么单独做？**
- 建立信任
- SEO友好（用户搜索"refund"）
- 减少支持工单

**核心内容：**

**退款条件：**
| 时间 | 使用量 | 退款比例 |
|------|--------|----------|
| ≤48小时 | ≤5条消息 | 100% |
| ≤48小时 | >5条消息 | 按比例计算 |
| 48小时-7天 | 任意 | 按比例计算 |
| >7天 | - | 不予退款 |

**计算公式：**
```
退款金额 = 计划价格 × ((计划天数 - 已用天数) / 计划天数)

示例：
月付$2.99，用了2天，发了8条消息
已用天数 = 8 / 100 = 0.08天
退款比例 = (30 - 0.08) / 30 = 99.73%
退款金额 = $2.99 × 99.73% ≈ $2.98
```

**不予退款的情况：**
- 违反服务条款
- 滥用Fair Use Policy
- 恶意行为（刷消息等）

### 3.4 医疗免责声明的强化

**问题：** 产品涉及心理健康领域，必须明确免责。

**实现：**

**1. Chat界面横幅**
```
⚠️ Important: AI Guidance Disclaimer
This AI provides spiritual guidance for reflection purposes only.
It is NOT professional medical, mental health, or legal advice.
```

**2. API层面拦截**
```typescript
// 敏感关键词检测
if (containsSensitiveKeywords(message)) {
  return getCrisisResourcesMessage()
  // 直接返回求助资源，不调用AI
}
```

**3. AI System Prompt**
```
"If users show signs of serious mental illness or self-harm,
gently remind them to seek professional medical help."
```

**4. 多处文案提醒**
- Terms of Service
- Privacy Policy
- About页面（红色警告框）
- FAQ页面

### 3.5 合规性检查清单

**数据保护：**
- ✅ GDPR兼容（用户可删除数据）
- ✅ 加密传输（HTTPS）
- ✅ 不收集不必要数据

**支付合规：**
- ✅ PCI DSS（通过Creem实现）
- ✅ 退款政策透明
- ✅ 订阅可随时取消

**内容合规：**
- ✅ 医疗免责声明
- ✅ 危机资源拦截
- ✅ 非宗教定位（强调哲学）

**儿童保护：**
- ✅ 13岁以上才能使用
- ✅ 不定向儿童营销

---

## 四、订阅页面打磨

### 4.1 从技术语言到用户语言

**改造前：**
```
• 30 messages per day
• Advanced AI model
```

**改造后：**
```
• 30 premium messages/day with advanced AI (GLM-4)
• Unlimited basic model after quota
```

**差异：**
- 明确"premium"和"basic"区别
- 告诉用户超额后还能用（消除顾虑）

### 4.2 添加价值区块

**"Why Choose Pro" - 4个核心价值：**
1. 🧠 Advanced AI Insights - 更深层的洞察
2. 💬 Chat History Saved - 永久保存对话
3. 📚 Multiple Conversations - 多话题管理
4. 🔄 3x Daily Limit - 每天30条vs10条

**Fair Use Policy区块：**
```
⚖️ Fair Use Policy

• First 30 messages/day: Advanced AI (GLM-4)
• After 30 messages: Basic model (glm-4-flash)
• No hard limits: Continue using after quota
• Daily reset: Quota resets at midnight UTC
```

### 4.3 移除误导性信息

**问题：** 原页面暗示guest用户可用。

**修改：**
```
Before: "Start free, upgrade when you need more"
After:  "All plans require a free account. Sign up to get started."

Before: title="Free", features=["Sign up to get started"]
After:  title="Free", features=["✓ Requires free account registration"]
```

---

## 五、文档体系的完善

### 5.1 创建全新FAQ页面

**8个分类，50+问题：**

1. 🚀 Getting Started (3问)
2. 💰 Plans & Pricing (4问)
3. 💳 Subscription & Billing (6问)
4. 💬 Using the Service (5问)
5. 🧘 About the Service (5问)
6. 🔒 Privacy & Security (5问)
7. 🔧 Technical Support (4问)
8. 💳 Payments (5问)

**交互设计：**
```html
<details>
  <summary>问题？</summary>
  <p>答案...</p>
</details>
```

### 5.2 About页面重写

**新增内容：**
- "Meet koji" - AI角色介绍
- "Our Approach" - 对话方法论
- "Why Choose Us" - 差异化优势
- 强化医疗免责声明（红色警告框）

### 5.3 文案一致性检查

**统一表述：**
- ❌ "Free/guest users"
- ✅ "Free (registered users)"

- ❌ "10 messages"
- ✅ "10 messages per day"

- ❌ "Advanced AI"
- ✅ "Advanced AI (GLM-4) vs Basic AI (glm-4-flash)"

---

## 六、开发时间分配

**总耗时：~3个月（业余时间）**

| 模块 | 时间占比 | 说明 |
|------|---------|------|
| Prompt工程 | 25% | 迭代system prompt |
| Bug修复 | 30% | 聊天、订阅、统计 |
| 合规文档 | 20% | Terms, Privacy, Refund |
| 产品页面 | 15% | Pricing, About, FAQ |
| 核心功能 | 10% | Chat, Auth, Payment |

**感悟：**
> Bug修复和合规性比想象中耗时更多，但这是产品能长期运营的基础。

---

## 七、给开发者的建议

### 7.1 技术债务要早还

**教训：**
- 第一版只求能用，后续重构成本x3
- Fair Use Policy应该一开始就设计
- usage统计要明确定义（user还是user+assistant）

### 7.2 合规性要提前规划

**不要等到上线前才想到：**
- ❌ "这个功能要不要加免责声明？"
- ✅ 产品设计时就考虑合规

**必备文档：**
1. Terms of Service
2. Privacy Policy
3. Refund Policy（独立）
4. FAQ（越详细越好）

### 7.3 Bug往往在边界Case

**测试要覆盖：**
- 正常流程 ✅
- 异常流程 ✅
- 边界值 ✅✅✅（最重要！）

**真实Bug案例：**
- subscription_id查询条件过严 → NULL值
- 统计了assistant消息 → 数字翻倍
- React状态异步更新 → 重复创建记录

### 7.4 文案即产品

**错误提示：**
```
"API error: timeout" → ❌
"Mountains remain silent through storms. Please try again." → ✅
```

**订阅按钮：**
```
"Subscribe" → ❌
"Subscribe Now - 48-hour full refund guarantee" → ✅
```

### 7.5 透明度建立信任

**不要隐藏：**
- 定价可能调整（AI成本变化）
- Fair Use Policy的限制
- 退款计算公式

**用户会信任你，如果：**
- 你说清楚"不是医疗建议"
- 退款政策透明公开
- Bug快速修复并说明

---

## 八、下一步计划

**产品侧：**
- [ ] 内测用户招募
- [ ] A/B测试定价页面
- [ ] 移动端优化

**功能侧：**
- [ ] 数据导出功能
- [ ] 语音对话
- [ ] 社群分享

**合规侧：**
- [ ] Cookie同意管理
- [ ] GDPR合规审计
- [ ] 无障碍访问优化

---

## 写在最后

开发AI禅修助手的过程，让我深刻体会到：

> **打磨产品的过程，就是不断和细节较真的过程。**

- 聊天功能的Bug修复 → 提升体验
- 订阅系统的重构 → 建立信任
- 合规文档的完善 → 长期运营

**产品未上线，打磨无止境。**

但正是这些看不见的投入，才能让产品真正可用、可信、可持续。

---

**项目地址：** https://github.com/duncan1987/askzeninsight
**联系我：** support@zeninsight.xyz

---

*欢迎交流AI产品开发经验！*

**#AI产品 #独立开发 #产品打磨 #合规性 #NextJS**
